export type Project = {
  name: string;
  date: Date;
  description: string;
  skills: string[];
};

export type StatusUpdate = {
  title: string;
  date: Date;
  location: string;
  update: string;
};
